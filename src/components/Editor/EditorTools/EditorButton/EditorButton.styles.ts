import { Box, Button, styled } from "@mui/material";

export const EditorButtonBoxContainer = styled(Box)({
  position: "absolute",
  marginRight: "600px",
});

export const EditorButtonClick = styled(Button)({
  minWidth: 48,
  width: 48,
  height: 48,
  fontSize: 24,
});
